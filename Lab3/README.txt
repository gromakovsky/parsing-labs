В папке obfuscator лежат исходники обфускатора и Makefile. При запуске без аргументов выводится help.
В папке Testing есть файлы с тестами, run_tests.sh запускает обфускатор на каждом из них.
В папке Report есть отчёт (report.pdf) и исходники для него.
